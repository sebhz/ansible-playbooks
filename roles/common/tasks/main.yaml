---
- name: Install packages
  apt:
    name:
      - autojump
      - build-essential
      - clang-format
      - git
      - neovim
      - pylint
      - screen
      - silversearcher-ag
      - stow
    state: present
  when: ansible_facts['os_family'] == "Debian"

- name: Clean package cache and dependencies
  apt:
    autoclean: yes
    autoremove: yes
  when: ansible_facts['os_family'] == "Debian"

